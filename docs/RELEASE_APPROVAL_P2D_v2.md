# RELEASE_APPROVAL_P2D_v2.md — قرار الإصدار النهائي (2026-07-11)

> تحديث لبوابة الاعتماد بعد اكتمال بروتوكول تحقّق B1 الخماسي. لا يكرّر التقارير السابقة
> ولا يفتح قرارات مُقرَّة. المرجعان: `RELEASE_APPROVAL_P2D.md` (v1 — REJECTED) و
> `docs/evidence/B1_ROOT_CAUSE.md`.

## 1) حالة B1

**مُغلق — CLOSED.** أُثبت عبر البروتوكول الكامل:

| المرحلة | النتيجة |
|---|---|
| 1 — السبب الجذري | موثَّق (`B1_ROOT_CAUSE.md`): افتراض «حياد الكتابة المزدوجة» أثناء تعايش المحرّكين كان خاطئاً؛ انتهاك ق3/م.16/م.11/م.4؛ الأثر التجاري: محو ذمّة حقيقية بقيمة ورقية. |
| 2 — تحقّق التراجع | حزمة R1–R9 المخصّصة (المسار الأصلي، الحدود 0.01/1,000,000، بيانات دنيا، تسلسلات مختلطة، العجز، دفعات الغداء، تصنيف كل صفّ، الإلغاء/التصحيح) — **38/38 PASS، صفر أخطاء**. |
| 3 — عبر المحرّكين | كل سيناريو على FIN القديم وFIN2 معاً؛ أسطح المحرّك القديم **جامدة تماماً** أمام أي إدخال عيني؛ **ثلاثة تباعدات مقصودة موثَّقة** (سلوك قديم سابق للوجود، توحيدها = P3) — لا شيء غير مُفسَّر. |
| 4 — تدقيق Fable 5 المستقل (المدخلات الثلاثة فقط) | **PASS** — «السبب الجذري أُزيل بنيوياً (الهوية القديمة تُصفَّر عند المصدر)؛ لا مسار عودة في التشغيل العادي (grep لكل كُتّاب الحقول القديمة: مسار الكتابة الوحيد `crud.js:130`)؛ أعدتُ تشغيل الحزمة بنفسي 38/38؛ **أوقّع الموافقة الإنتاجية**.» |

## 2) المخاطر المتبقية

| الخطر | الدرجة | المعالجة |
|---|---|---|
| مسار إداري كامن: `reclassifyVoucher` يُعيد تصنيف تبرّع نقدي قديم إلى عيني دون تصفير حقول العرض القديمة — يُعيد نمط B1 كبيانات | منخفض (لا واجهة ولا مُستدعٍ له إطلاقاً — grep؛ إداري بسبب إلزامي وموثَّق بالكامل) | **شرط ما بعد النشر #1** |
| التباعدات القديمة الثلاثة الموثَّقة (الديوان القديم يتجاهل تبرّعاته الموجَّهة؛ تقسيم دَين-أولاً للتبرع المرتبط بعضو؛ إعادة التخصيص الديناميكية للفائض) | متوسط | **شرط ما بعد النشر #2 (P3)** |
| لا استعادة آمنة بعد (المعطَّلة تحمي) | متوسط | مجدولة (P5) |
| إعادة تصنيف بمدير واحد؛ لا فهرس فريد لأرقام السندات | منخفض | مجدولة (P3/P5) |

## 3) الحكم الإنتاجي النهائي

# ⚠ APPROVED WITH CONDITIONS

## 4) الشروط الواجب احترامها بعد النشر

1. **قفل `reclassifyVoucher` الإضافي قبل أي كشف واجهة (P3):** إعادة التصنيف إلى حدث غير نقدي
   يجب أن تُصفِّر حقلَي العرض القديمَين على الصفّ (أو تُرفَض إعادة تصنيف أحداث التبرّع حتى توحيد P3).
   حتى ذلك الحين: لا يُستدعى `reclassifyVoucher` على تبرّعات قديمة إلا بقرار مالك موثَّق.
2. **توحيد P3 هو الأولوية التالية الأولى:** إنهاء التباعدات القديمة الثلاثة (الإيصال الشفّاف،
   أحداث التسوية، محرّك تخصيص واحد) — كلّها ضمن خارطة FMD-01 المُقرَّة.
3. **حزمة القبول `tests/e2e-acceptance.cjs` (38 تأكيداً) تُشغَّل قبل كل إصدار قادم** — هي الحارس
   الدائم ضد عودة B1 وفئته.
4. **قاعدة النافذة تبقى حتى الدمج:** أي سند يُدخَل على الإنتاج قبل نشر هذا الإصدار يتبع قاعدة
   بوابة P2-D (المُصنِّف الآمن بعد الإدخال).

## 5) التجميد (يسري باعتماد المالك ودمج PR #33)

يُوصى رسمياً بتجميد:
- **المعمارية المالية** و**النموذج المالي** (الخزائن الثلاث + السجلّان + التصنيف الصريح)؛
- **المحرّك المالي** (`fin.js` = `47ab43d3e86bca33c295413d7fcaf473` المجمّد أصلاً، و`fin2.js`/`model2.js` كواجهة النموذج الجديد)؛
- **الوثائق الحاكمة**: FAR-01 · FLR-01 · FEM-01 · FAS-01 · FMD-01.

**قاعدة التغيير الدائمة:** أي تغيير مالي مستقبلي يبدأ **بتعديل الوثائق الحاكمة** واعتمادها،
قبل لمس أي تنفيذ.
