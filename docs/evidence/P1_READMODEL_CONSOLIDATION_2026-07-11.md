# P1 — Read-Model Consolidation (2026-07-11)

**الهدف:** جعل كل رقم مالي في طبقة العرض يُقرأ من مصدر واحد موثوق (`FIN`)، وحذف الحسابات
المكرّرة **فقط**. **لا تغيير** في أي قاعدة عمل مالية، منطق تخصيص، سلوك خزائن، أرصدة أعضاء،
أو بيانات تاريخية. **لا تغيير في أي قيمة معروضة.**

| الحقل | القيمة |
|---|---|
| المرحلة | **P1 — توحيد نموذج القراءة** (بلا تغيير قيمة) |
| القاعدة | مبنية على خط الأساس `FINANCIAL_BASELINE_2026-07-11.md` (P0) |
| بصمة `fin.js` | `47ab43d3e86bca33c295413d7fcaf473` (**دون تغيير** — لم يُمَسّ المحرّك) |

---

## 1) جرد طبقة العرض — أين تُحسب الأرقام المالية؟

فحص لكل سطح (لوحة تحكم · كشوف · تقارير · تصدير · طباعة):

| السطح | المصدر قبل P1 | الحالة |
|---|---|---|
| كشف الصندوق (`renderStmt`) | `FIN.fundLedger` + `FIN.foodBalance/foodDeficitRemaining/foodNetPosition/…` | ✅ FIN بالفعل |
| كشف العضو (`renderMemberStmt`) | `FIN.memberStatement` | ✅ FIN بالفعل |
| تقرير المديونية (`annualDebtRows`) | `FIN.memberStatement` | ✅ FIN بالفعل |
| تقرير المتأخرين (`delinquentRows`) | `FIN.memberDelinquency` | ✅ FIN بالفعل |
| كشف التبرعات (`prtDonStmt`) | `FIN.allocateFoodDonations` + `FIN.food*Total` | ✅ FIN بالفعل |
| صفحة النسخ (`renderSysInfo`) | `FIN.foodBalance/diwanBalance` | ✅ FIN بالفعل |
| مؤشّرات KPI باللوحة | `FIN.foodBalance/foodDeficitRemaining/foodNetPosition/diwanBalance` | ✅ FIN بالفعل |
| **لوحة الخزينة — إجمالي الغداء** | `FIN.foodBalance()` | ✅ FIN بالفعل |
| **لوحة الخزينة — إجمالي الديوان** | `opening+income−expense` (مكرّر يدوي) | 🔧 **وُحِّد → `FIN.diwanBalance()`** |
| **لوحة الخزينة — إجمالي التبرعات** | `Σ(donation receipts)` (مكرّر يدوي) | 🔧 **وُحِّد → `FIN.donBalance()`** |

## 2) التغييرات المطبَّقة (مطابقة القيمة تماماً)

1. **إجمالي خزينة الديوان** (`app.js`): `total=opening+income−expense` → `total=FIN.diwanBalance()`.
   المعادلتان متطابقتان حرفياً؛ تبقى `income/expense` لعرض التدفّق فقط.
2. **إجمالي خزينة التبرعات** (`app.js`): `total=income` → `total=FIN.donBalance()`.
   `FIN.donBalance()` = نفس مجموع صفوف التبرعات غير المحذوفة.

> لم يُمَسّ `fin.js` (بصمته ثابتة). التوحيد تمّ باستدعاء دوال FIN **الموجودة** فقط.

## 3) إثبات صفرية التغيّر (بيانات إنتاج حقيقية)

قراءة القيم **المعروضة فعلاً** في لوحة الخزينة ومؤشّرات KPI، قبل (main/P0) وبعد (P1):

| القيمة المعروضة | قبل (P0) | بعد (P1) | الحكم |
|---|---|---|---|
| إجمالي خزينة الديوان | ₪ 3,481 | ₪ 3,481 | ✅ متطابق |
| إجمالي خزينة الغداء | ₪ 1,694 | ₪ 1,694 | ✅ متطابق |
| إجمالي خزينة التبرعات | ₪ 2,364 | ₪ 2,364 | ✅ متطابق |
| KPI: غداء / عجز / صافي / ديوان | 1,694 / −8,139 / −6,445 / 3,481 | نفسها | ✅ متطابق |

الدخان (Playwright): صفر أخطاء صفحة/كونسول؛ كل الأسطح تُعرض.

## 4) بنود مؤجَّلة (خارج P1 لأنها **تُغيّر قيمة** معروضة)

هذه ليست تكراراً مطابقاً لدالة FIN، بل مصادر مختلفة قد تُنتج قيمة مختلفة، فتوحيدها يُغيّر
الرقم المعروض — وهو ممنوع في P1. تُرحَّل إلى مرحلة لاحقة **باعتماد صريح**:

1. **حلقة نسبة التحصيل** باللوحة تقرأ العمود المخزّن `balance_ils` لتحديد «مَن دفع»، بدل
   اشتقاقها من `FIN.memberDelinquency` (due/paid). المصدران قد يختلفان → توحيدهما يُغيّر النسبة.
2. **مقام نسبة أشرطة KPI** يجمع `|fb|+|rd|+|np|+|db|` مع أن `np≡fb+rd` (ازدواج حسابي في
   نسبة بصرية فقط). تصحيحه يُغيّر عرض النِّسَب.

> توصية: تُعالَج ضمن مرحلة تصحيح لاحقة (ليست «توحيد قراءة»)، لأن كلاهما يُصحّح قيمة لا يوحّد مصدراً.

## 5) الخلاصة

- كل الأسطح المالية تقرأ الآن من `FIN` كمصدر وحيد؛ لم يتبقَّ حساب مكرّر لأي إجمالي خزينة موثوق.
- لا قيمة واحدة تغيّرت (إثبات على بيانات الإنتاج).
- `fin.js` دون تغيير؛ لا مساس بقواعد العمل/التخصيص/الخزائن/الأرصدة/التاريخ.
