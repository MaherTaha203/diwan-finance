# Migration Verification Report — P2-C (2026-07-11)

> صدر **فور اكتمال الترحيل الفعلي** على قاعدة الإنتاج `ralifvemgapmsgrjgazh`، وفق الضمانة
> الثالثة للمالك. القاعدة الحاكمة: **الترحيل ناجح فقط إذا بقيت كل القيم المالية متطابقة؛
> يتغيّر التصنيف وحده؛ النيّة التاريخية لا تتغيّر؛ النقد لا يُخلق ولا يختفي.**

## 0) ما نُفِّذ على القاعدة

| الخطوة | التفصيل | النتيجة |
|---|---|---|
| إعادة تحقّق اللقطة الطازجة (قبل الكتابة) | بصمات md5 الأربع مقابل النسخة المُتحقَّقة | **مطابقة** (`202018d9`/`ab74ce2a`/`527e80a7`/`e3013fa4`) — نافذة التجميد قائمة |
| DDL (`p2c_classification_layer`) | 5 أعمدة nullable للإيصالات + 4 للمدفوعات — **إضافية فقط** | ✅ لا عمود قائم مُسّ |
| الكتابة المحروسة | 11 عبارة `UPDATE … WHERE movement_type IS NULL` مطابقة للملحق أ | ✅ 58 إيصالاً + 26 صرفاً؛ **صفر غير مُصنَّف** |
| توزيع المجموعات | 23 دفعة عضو · 18+1+1 تبرع نقدي · 7 عيني/خدمي · 8 وسم محذوف · 25+1 مصروف | ✅ مطابق لخطة المحاكاة حرفياً |
| **Idempotency على القاعدة الفعلية** | إعادة تشغيل الكتلة كاملة ثم بصمة حالة التصنيف | ✅ `4f21d0c7…` قبل = بعد؛ صفر تغيّر |

## 1) Treasury Verification — الخزائن الثلاث

محسوبة مرّتين بطريقتين مستقلّتين: SQL مباشرة على القاعدة، وتشغيل التطبيق الكامل (المحرّكان القديم والجديد):

| الخزينة | المتوقَّع (المحاكاة المعتمدة) | SQL من القاعدة | التطبيق — FIN القديم | التطبيق — FIN2 الجديد | الحكم |
|---|---|---|---|---|---|
| الغداء | 1,694.04 | 1,694.04 | 1,694.04 | 1,694.04 | ✅ |
| الديوان | 3,481.00 | 3,481.00 | 3,481.00 | 3,481.00 | ✅ |
| العجز التاريخي | −8,139.00 | −8,139.00 | −8,139.00 | −8,139.00 | ✅ |

**النموذجان القديم والجديد متطابقان تماماً** — لا قيمة نقدية تغيّرت. تسويات العجز = 0 (لم يحدث أي صرف تسوية بعد — صحيح).

## 2) Donation Verification — السجلّان

| السجل | المتوقَّع | الفعلي (SQL + التطبيق) | الحكم |
|---|---|---|---|
| سجل التبرعات النقدية | 8 قيود = ₪ 5,200.00 | 8 = 5,200.00 (4,700 ديوان + 500 عجز) | ✅ |
| السجل العيني والخدمي | 4 قيود = ₪ 1,863.72 (توثيقي) | 4 = 1,863.72 · **أثر نقدي صفر** | ✅ |

## 3) Member Verification — أرصدة الأعضاء الـ151

| المقياس | قبل الترحيل | بعد الترحيل | الحكم |
|---|---|---|---|
| مجموع الأرصدة النهائية | 226,784.96 | **226,784.96** | ✅ |
| بصمة كل الأرصدة (151 عضواً) | md5 | **متطابقة** | ✅ |

## 4) Reports Verification — التقارير

لقطة شاملة من التطبيق (قبل مقابل بعد، نفس الكود): كشف الغداء، كشف الديوان، كشف العضو،
تقرير المديونية (بصمة النص كاملاً)، تقرير المتأخرين (بصمة النص كاملاً)، لوحة الخزينة، KPI —
**كل قيمة متطابقة، صفر فروقات**. (الواجهة القديمة لا تقرأ أعمدة التصنيف أصلاً حتى P2-D.)

## 5) Print Verification — الطباعة

مبالغ كشف حساب العضو المطبوع (12 قيمة ₪ مُلتقطة من قالب الطباعة الفعلي): **بصمة متطابقة قبل/بعد** ✅.
بقية القوالب تقرأ من نفس دوالّ `FIN.*` المُثبت ثباتها في §1/§4.

## 6) Export Verification — التصدير

| القناة | طريقة التحقّق | النتيجة |
|---|---|---|
| Backup (JSON) | التقاط الملف المُصدَّر فعلياً | ✅ يعمل؛ حجمه كبر **عمداً** (يحمل الآن أعمدة التصنيف — القيم المالية فيه مطابقة بصمةً) |
| PDF / الطباعة | التقاط مبالغ القالب المُصيَّر | ✅ متطابقة (§5) |
| Excel / CSV | تحقّق على مستوى مصدر البيانات: المُنشئات تقرأ الصفوف الخام (بصمتها ثابتة §7) + دوالّ FIN (ثابتة §1) | ✅ مدخلاتها متطابقة بايت-بايت |

## 7) Historical Integrity — التاريخ لم يُمَسّ (الإثبات القاطع)

بصمات md5 للأعمدة المالية الأصلية (رقم/تاريخ/مبلغ/صندوق/عضو/حذف) **بعد** الترحيل:

| الجدول | قبل | بعد | الحكم |
|---|---|---|---|
| receipts | `202018d9…` | `202018d9…` | ✅ بايت-بايت |
| payments | `ab74ce2a…` | `ab74ce2a…` | ✅ |
| member_subscriptions | `527e80a7…` | `527e80a7…` | ✅ |
| members | `7412290b…`/`e3013fa4…` | متطابقة | ✅ |

**لا سند أُعيدت كتابته. تغيّر التصنيف وحده (طبقة أعمدة جديدة).** `fin.js` المجمّد: `47ab43d3…` دون تغيير.

## 8) حفظ النقد

```
الوارد النقدي = 10,257.76 − 1,863.72 (قيم توثيقية عيني/خدمي) = 8,394.04
Σ(الخزائن) = 1,694.04 + 3,481.00 − 8,139.00 = −2,963.96
           = (−8,639) + 10,481 + 8,394.04 − 13,200.00 = −2,963.96  ✓
```
**لا نقد خُلق، لا نقد اختفى، ولا خزينة استلمت قيمة غير نقدية.**

## 9) قفل التصنيف (الضمانة 2 — نافذ)

- لا مسار كتابة عادي يمسّ حقول التصنيف (مُتحقَّق بالفحص: المُصنِّف يُعرِّف، `fin2` يقرأ، لا غير).
- الاستثناء الوحيد: `reclassifyVoucher` — إداري فقط، سبب إلزامي، حفظ التصنيفَين السابق والجديد
  في `voucher_versions`، قيد تدقيق إلزامي (تاريخ/مستخدم/سبب). لا شيء يُستبدل بصمت.
- الملحق أ وثيقة دائمة: `docs/evidence/APPENDIX_A_RECLASSIFICATION_LEDGER.md`.

## 10) Fable 5 Final Gate — الشهادة النهائية

_قيد المراجعة المستقلّة — تُدرَج هنا. لا يُعلَن اكتمال P2-C قبل شهادتها._
