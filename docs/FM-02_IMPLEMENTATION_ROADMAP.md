# FM-02 — مخطّط التنفيذ حسب مجالات العمل المستقلّة (Execution Blueprint · v4)
# FM-02 — Independent Domain-Based Execution Blueprint

> **الحالة:** مخطّط التنفيذ الرسمي المُقترَح — بانتظار مراجعة المالك واعتماده النهائي. **لا تنفيذ بعد.**
> **المرجع الأعلى:** دستور FM-01 v3.1 (المجمَّد، **دون أي تعديل**)؛ عند أي تعارض يعلو الدستور.
> **التاريخ:** 2026-07-12.
> **الغاية:** أربعة مجالات أعمالٍ **مستقلّةٌ على مستوى الكود** (لا يستدعي أيٌّ منها كودَ آخر)،
> **مقترنةٌ فقط بعقد الحدث المالي المجمَّد** في الطبقة التحتية الواحدة. من أراد تعديل «الغداء» يفتح
> مجال الغداء وحده فيجد كل ما يخصّه، دون فتح أي مجالٍ آخر ودون معرفة تاريخ المشروع.

---

## أولاً — قرار معماري: المحرّك المالي **طبقة تنفيذ**، وليس مجال أعمال
**المستخدم لا يعرف FIN ولا FIN2.** يعرف: الديوان، الغداء، التبرعات، العضو، الصندوق. أما المحرّك
فبنيةٌ داخلية. لذلك:
> **المحرّك المالي ليس Business Domain، بل «طبقة الحدث المالي» (Financial Event Layer) تعمل
> أسفل جميع المجالات.**

**التبرير المعماري (قويّ):**
1. **معيار المجال:** المجال يقابل شيئاً يعرفه المستخدم. المحرّك لا يقابل شيئاً يعرفه ⇒ ليس مجالاً.
2. **دور المحرّك دستورياً (FM-01 §4.0):** أن يكون المصدر الوحيد الذي **تشتقّ منه كل المجالات**
   حقائقها. هذا **تعريف الطبقة التحتية المشتركة**، لا تعريف مجالٍ نظير.
3. **لو كان مجالاً لخالف الاستقلال:** لصار كل مجالٍ «يعتمد على مجال المحرّك». أما كطبقةٍ تحت
   الجميع، فالمجالات تقترن **بالطبقة لا ببعضها** — وهذا عين الاستقلال المطلوب.
4. **توحيد FIN/FIN2 عملٌ داخل الطبقة**، غير مرئيٍّ للمجالات (مطابقةٌ بايتية)، يُنفَّذ **أخيراً**
   دون أن يمسّ أي مجالٍ مُغلق (يحقّق رغبة «المحرّك أخيراً» بلا جعله مجالاً).

**عقد الطبقة (Financial Event Access Contract):** كل مجالٍ يقرأ أرقامه ويُصدِر أحداثه **فقط** عبر
دوال الطبقة الثابتة المشتقّة من الحدث المالي الواحد؛ لا يقرأ محرّكاً بعينه ولا مجالاً آخر. ثبات
هذا العقد هو ما يُبقي المجالات مستقلّةً ومُغلقة.

**تفكيك «الالتقاط» (تصحيح تدقيق A):** لتفادي أي لبس، يُفصَل الحدث إلى ثلاث مسؤوليات:
1. **تعريف الحدث (شكله السلكي):** تملكه **الطبقة/الدستور** (`MODEL2` المجمَّد) — لا يتغيّر إلا بتعديل الدستور.
2. **إطلاق الحدث (المُحفِّز + النموذج + التحقّق):** يملكه **المجال** التجاري صاحب العملية.
3. **تخزينه وواجهة قراءته:** تملكهما **الطبقة**.
> بهذا يكون الاقتران بين المجالات **عبر عقد الحدث المجمَّد في الطبقة فقط** (Published Language)، لا
> بين المجالات مباشرة. **الطبقة نواةٌ مشتركة تحمل قواعد أعمالٍ دستورية** (تخصيص البند 9، الفائض،
> ق4/ق5)، لا سباكةً محايدة — ولذلك وجب تجميد عقدها دستورياً (تصحيح B).

---

## ثانياً — مبدأ الاستقلال (على مستوى الكود)
- **لا مجال يستدعي كودَ مجالٍ آخر.** كل مجالٍ نظامٌ صغيرٌ مكتمل يقترن بطبقة الحدث المالي فقط.
- **الإحالات بين المجالات قراءةٌ فقط عبر الطبقة** (لا استدعاء كودٍ لمجالٍ آخر). حين يعرض «سجل
  التبرعات» تبرعاً وجهتُه الغداء، فهو يقرأ **الحدث** من الطبقة، لا كودَ مجال الغداء. هذه إحالةٌ
  مقصودة ومبرَّرة (منظرٌ على مخزن الأحداث)، وليست تبعية كود.
- **الصياغة الدقيقة (تصحيح A):** الاستقلال هنا **استقلالُ كودٍ وقابليةِ تعديلٍ وإغلاق**، لا انعدامَ
  اقترانٍ مطلق. الاقتران الوحيد الباقي هو **عقد الحدث المجمَّد في الطبقة** (تغييرُ شكل حدثٍ يتطلّب
  تعديل الدستور، وحينها فقط تتأثّر المناظر القارئة — انظر خارطة التبعيات §6). هذا اقترانٌ **مُصغَّر
  ومركزيٌّ في الطبقة**، لا اقترانٌ بين المجالات.
- **الاستثناء الوحيد المحكوم لقاعدة «قراءة فقط»:** إعادة التصنيف الإدارية — عمليةٌ في **الطبقة**
  تعمل بانتظامٍ على عقد الحدث (تصحيح D)، لا كتابةٌ من مجالٍ في آخر.

---

## ثالثاً — تعريف «اكتمال المجال» (Definition of Done — قائمة تحقّق موحّدة)
لا يُغلق أي مجالٍ إلا بعد اجتياز **كل** البنود:
1. ☐ جميع الشاشات مكتملة.
2. ☐ جميع الكشوف التي يملكها مكتملة.
3. ☐ جميع التقارير التي يملكها مكتملة.
4. ☐ جميع الطباعة الخاصة به مكتملة.
5. ☐ جميع التصدير والبحث/المرشِّحات مكتملة.
6. ☐ المصطلحات/الأيقونات/نصوص المساعدة موحّدة (لا بقايا قديمة).
7. ☐ جميع الصلاحيات مكتملة (من يرى/يُنشئ/يعتمد).
8. ☐ **إنفاذ مبدأ الأثر المحاسبي (FM-01 §4.2)** على كل سطوحه.
9. ☐ جميع الاختبارات PASS (قبول + انحدار + مطابقةٌ بايتية عند أي مساسٍ بالأرقام).
10. ☐ خطة Rollback مثبتة والبيانات الإنتاجية محفوظة.
11. ☐ جميع الوثائق محدّثة.
12. ☐ مراجعة مستقلة PASS (Fable 5).
13. ☐ اعتماد المالك الصريح.
**بعدها فقط يُغلق المجال نهائياً**، ولا يُعاد إليه إلا لإصلاح خطأ.

---

## رابعاً — المجالات الأربعة (كلٌّ بمصفوفة ملكيته)

### المجال 1 — إدارة الديوان
**مصفوفة الملكية**
| البُعد | ما يملكه المجال 1 |
|---|---|
| السندات (الأحداث) | إيصال الديوان التشغيلي · تبرع الديوان النقدي · مصروف الديوان · استرداد سند ديوان |
| الخزائن | خزينة الديوان |
| السجلّات | — *(لا يملك سجلّات؛ تبرعه النقدي يظهر في سجل مجال التبرعات كمنظرٍ عبر الطبقة)* |
| الحسابات | حساب صندوق الديوان (تشغيلياً) |
| الكشوف | كشف صندوق الديوان |
| التقارير | تقارير الديوان |
| الطباعة | سند الديوان · كشف الديوان |
| الاختبارات | التشغيلي لا يدخل السجل النقدي · التبرع يدخله · أرصدة الديوان القائمة ثابتة |
- **الملفات:** `crud.js` · `forms.js`/`index.html` · `migrate2.js` · `app.js` (`diwan-*` + تبويب خزينة الديوان) · `print.js` · `reports.js` · `data.js` (`DIWAN_OPENING`).
- **خطة العكس:** إخفاء النوع التشغيلي الجديد (التاريخي بلا مساس). **الأثر المالي:** صفر على القائم.
- **بوابة الإغلاق:** قائمة الاكتمال كاملةً + إذن صريح.

### المجال 2 — إدارة صندوق الغداء
**مصفوفة الملكية**
| البُعد | ما يملكه المجال 2 |
|---|---|
| السندات (الأحداث) | اشتراك الغداء · إيصال الغداء · تبرع غداء موجَّه (غداء/عجز) · تحصيل ذمة (ق4) · تسوية العجز · مصروف الغداء · استرداد سند غداء · التخصيص اليدوي P7 |
| الخزائن | خزينة الغداء |
| السجلّات | — *(تبرعاته النقدية تظهر في سجل مجال التبرعات كمنظر؛ ق4/ق5 مستبعدة منه بحكم الدستور)* |
| الحسابات | حساب صندوق الغداء · **حساب تسوية العجز التاريخي** (السلوك + المستفيد الواحد + مسار التسوية) |
| الكشوف | كشف صندوق الغداء · كشف تسوية العجز |
| التقارير | تقارير الغداء · المديونية · الأعضاء المتأخرون *(مستحقّات اشتراكات الغداء)* |
| الطباعة | سند الغداء **الشفّاف** (تفصيل التوزيع + الرصيد الناتج) · كشف الغداء/العجز |
| الاختبارات | ق1أ/ق4/ق5 · إعادة تسمية حساب التسوية بمطابقةٍ بايتية · تدقيق P7 · «لا صرف تشغيلي من حساب التسوية» |
- **الملفات:** `crud.js` (`isQ4Collection`، P7، تسوية العجز) · `forms.js`/`index.html` · `migrate2.js` · `app.js` (`food-*` + تبويبا الغداء والعجز) · `print.js` · `reports.js` · `data.js` (`FOOD_OPENING`, `TREASURY_OPENINGS`).
- **خطة العكس:** استرجاع النصوص/إخفاء الواجهات الجديدة؛ لا بيانات تُمَسّ. **الأثر المالي:** صفر (تسمية + سلوكيات قائمة).
- **بوابة الإغلاق:** قائمة الاكتمال + ق4/ق5 خضراء + تدقيق P7 + إذن صريح.

### المجال 3 — إدارة التبرعات
**مصفوفة الملكية**
| البُعد | ما يملكه المجال 3 |
|---|---|
| السندات (الأحداث) | تبرع عيني/خدمي *(الالتقاط الوحيد الذي يملكه — لا يمسّ خزينة)* · *(استرداد التبرع النقدي يلتقطه مجالُ خزينة الوجهة، لا هذا المجال)* |
| الخزائن | — *(السجلّات لا تملك نقداً)* |
| السجلّات | **سجل التبرعات النقدية** (منظرٌ على أحداث التبرع النقدي عبر الطبقة) · **السجل العيني والخدمي** |
| الحسابات | — |
| الكشوف | — |
| التقارير | تقارير/كشوف التبرعات · البحث بالفئة/الوجهة |
| الطباعة | كشف التبرعات (يفصل النقدي عن التوثيقي) |
| الاختبارات | استبعادات ق4/ق5 من السجل النقدي · العيني توثيقيٌّ بلا نقد · تصحيح الإجمالي المطبوع (§4.2) |
- **الملفات:** `fin2.js` (`cashDonationRegister`/`inkindRegister` عبر الطبقة) · `app.js` (صفحة التبرعات + تبويب السجلات) · `reports.js` (`prtDonStmt`) · `print.js` · `forms.js`/`index.html` (نموذج العيني).
- **خطة العكس:** استرجاع القوالب؛ لا بيانات تُمَسّ. **الأثر المالي:** صفر (سجلّات بلا نقد).
- **بوابة الإغلاق:** مطابقة السطوح للمصفوفة (§5) + إذن صريح.

### المجال 4 — الحسابات والكشوف (العضو والمطابقة)
**مصفوفة الملكية**
| البُعد | ما يملكه المجال 4 |
|---|---|
| السندات (الأحداث) | — *(لا يملك التقاطاً؛ إعادة التصنيف عمليةُ **طبقة** محكومة، يستدعيها هذا المجال عبر واجهةٍ إدارية — تصحيح D)* |
| الخزائن | — |
| السجلّات | — |
| الحسابات | **حساب العضو** (المنظر الشخصي عبر الصناديق) · مطابقة الأرصدة بين كل الحسابات |
| الكشوف | **كشف حساب العضو** · لوحة مطابقة الأرصدة |
| التقارير | تقارير المطابقة (صحّة المتطابقات على مستوى النظام) |
| الطباعة | كشف العضو |
| الاختبارات | المتطابقات صامدة (صافي الغداء = الغداء + العجز؛ Σ الحركات = الكشف) · استدعاء إعادة التصنيف يحفظ الأثر ويُدقَّق |
- **الملفات:** `app.js` (`renderMemberStmt` + لوحة المطابقة) · `print.js` (`prtMemberStmt`) · `reports.js` · **واجهةُ استدعاء** إعادة التصنيف (العملية نفسها في الطبقة).
- **خطة العكس:** استرجاع القوالب/إخفاء أداة إعادة التصنيف. **الأثر المالي:** صفر (عرضٌ ومطابقة).
- **بوابة الإغلاق:** إثبات المطابقات + تدقيق إعادة التصنيف + إذن صريح.

> **إثبات عدم الازدواج (رابعاً + ثالثاً):** لا عنصرٌ مملوكٌ لأكثر من مجال. الإحالات الوحيدة
> بين المجالات هي **مناظر قراءةٍ عبر الطبقة** (سجل التبرعات يقرأ أحداث الديوان/الغداء؛ كشف العضو
> يقرأ أحداث العضو؛ المطابقة تقرأ الجميع) — **مقصودةٌ ومبرَّرة** (مناظر على مخزن الأحداث)، وليست
> ملكيةً مزدوجةً ولا تبعية كود.
>
> **حسمُ خيط التخصيص (تصحيح C):** قواعد التخصيص وق4/ق5 **قواعدُ دستورية** (FM-01)، ومحرّكُ
> تنفيذها (حساب ذمّة العضو والتجزئة) في **الطبقة**؛ ولذا حين يقرأ كشفُ العضو (المجال 4) وتقريرُ
> المتأخرين (المجال 2) ناتجَها، فكلاهما **منظرٌ على الطبقة** لا على الآخر. مجالُ الغداء يملك **فقط**
> الالتقاطَ والتحقّقَ الغذائيَّ الذي **يُطلِق** هذه القواعد، لا كودَها. فلا خيطَ تبعيةٍ بين المجالين.

---

## خامساً — طبقة الحدث المالي (الأساس المشترك — ليست مجالاً)
- **طبيعتها:** **نواةٌ مشتركة تحمل قواعد أعمالٍ دستورية** (تخصيص البند 9، الفائض، ق4/ق5)، لا سباكةً
  محايدة — ولهذا عقدها مجمَّدٌ دستورياً (تصحيح B).
- **دورها:** تعريفُ الحدث (شكله)، وتخزينُه، وكشفُ **عقد الوصول الثابت** الذي تقرأ منه كل المجالات؛
  ومحرّكُ التخصيص المنفِّذ للقواعد الدستورية؛ و**عملية إعادة التصنيف الإدارية** — العملية الوحيدة
  التي تكتب عبر الأحداث بانتظام (الاستثناء المحكوم لقاعدة القراءة-فقط، بحماية فصل المُعِدّ عن
  المُعتمِد + تدقيق كامل + حفظ الأثر) — تصحيح D.
- **يُنفَّذ على طرفين:**
  - **الأساس-أ (أولاً، قبل المجالات):** تعريف عقد الوصول (دوال ثابتة) فوق المحرّك الحالي؛ ولأن
    `FIN`/`FIN2` **يتطابقان اليوم فقط ويختلفان في ثلاثة مواضع موثَّقة** (الديوان القديم يتجاهل
    التبرعات الموجَّهة؛ `foodDeficitRemaining` غير مُسقَّف تحت الصفر مقابل السقف+الفائض في الجديد؛
    تجزئة الذمّة أولاً — أُغلقت بق5)، **يجب أن يُثبّت الأساس-أ الدلالةَ المعتمَدة لكلٍّ منها في نصّ
    العقد الآن** (اختيار القاعدة الفائزة)، وإن كانت بيانات اليوم تُخفي الفارق — وإلا كان العقد
    «ثابتاً ظاهرياً غامضاً باطناً» (تصحيح F). بلا أي تغيير سلوكي على بيانات الإنتاج.
  - **الأساس-ب (أخيراً، بعد إغلاق المجالات):** توحيد `FIN`/`FIN2` وإزالة التكرار **خلف العقد**،
    بمطابقةٍ **بايتية على مظروف بيانات الإنتاج الحالي، بإثبات انحدارٍ مسمّى** (لا مطلقاً — توحيد
    محرّكين مختلفين يفرض اختياراً قد يغيّر رقماً في حالة فيضٍ مستقبلية، وقد ثُبِّت في الأساس-أ)؛ ثم
    تجميد المحرّك دون أن يُعاد فتح أي مجال.
- **الملفات:** `fin.js` · `fin2.js` · `foodDonationAllocation.js` · `migrate2.js` · `data.js` +
  دوال العقد + `reclassifyVoucher` (عملية الطبقة). `model2.js` مرجع.
- **الأثر المالي:** صفر على مظروف الإنتاج (مُثبَتٌ بالانحدار المسمّى). **العكس:** المحرّك القديم خلف رايةٍ حتى ثبات المطابقة.

---

## سادساً — خارطة التبعيات (أثر أي تعديلٍ مستقبلي)
مفتاح الأمان: المجالات تقرأ من الطبقة فقط، فلا يؤثّر تعديلٌ في مجالٍ على آخر **إلا** إن غيّر **شكل
حدثٍ** تقرؤه مناظرُ مجالٍ آخر.

| إذا عُدِّل… | قد يتأثّر | ومتى فقط |
|---|---|---|
| **عقد طبقة الحدث** | **كل المجالات** | يتطلّب تعديل الدستور (FM-01 §4.0) — أعلى حساسية |
| **إعادة التصنيف (كتابةُ طبقةٍ محكومة)** | **كل مجالٍ يقرأ الحدث المُعاد تصنيفه** (خزينته وكشوفه) | كتابةٌ عابرة بحكم التصميم — لذا في الطبقة تحت حماية فصل المُعِدّ عن المُعتمِد (تصحيح D/E) |
| باطن المحرّك (الأساس-ب) | لا أحد **على مظروف الإنتاج** | مطابقةٌ بايتية خلف العقد (لا تُطلَق إلا بعد تثبيت دلالات الانحرافات في الأساس-أ — تصحيح F) |
| الديوان — شكل أحداثه | التبرعات (السجل النقدي)، الحسابات (كشف العضو إن تبرّع للديوان) | فقط عند تغيّر شكل الحدث |
| الديوان — واجهته/كشفه فقط | لا أحد | تغييرٌ داخلي |
| الغداء — شكل أحداثه | التبرعات (السجل النقدي)، الحسابات (كشف العضو/المطابقة) | فقط عند تغيّر شكل الحدث |
| الغداء — كشوفه/واجهته فقط | لا أحد | تغييرٌ داخلي |
| التبرعات — مناظره فقط | لا أحد | لا يُصدِر أحداثاً (عدا التقاط العيني) |
| التبرعات — شكل حدث العيني | الحسابات (إن ظهر في كشف) | فقط عند تغيّر شكل حدث العيني |
| الحسابات — عرضه/مطابقته فقط | لا أحد | يقرأ فقط، لا يُصدِر |
> **الخلاصة:** تغييرُ واجهة/كشف/تقرير مجالٍ لا يؤثّر على أحد. الأثر ينتقل **حصراً** عبر «شكل
> الحدث» في الطبقة — وهو محميٌّ بالدستور. هذا هو ضمان الاستقلال قابلاً للقياس قبل أي تعديل.

---

## سابعاً — تسلسل التنفيذ
**الأساس-أ (عقد الوصول، بلا سلوك)** → **الديوان** → **الغداء** → **التبرعات** → **الحسابات**
→ **الأساس-ب (توحيد المحرّك، مطابقةٌ بايتية، تجميد)**. كل مجالٍ يجتاز البوابات الأربع وقائمة
الاكتمال وينال إذناً صريحاً قبل إغلاقه؛ والمجالات مستقلّة، فترتيبها بينها مرنٌ عدا الأساس-أ أولاً
والأساس-ب أخيراً.

## القيد النهائي
**لا يبدأ أي مجالٍ (ولا الأساس) إلا بإذن المالك الصريح.** بعد اعتماد هذه المراجعة تصبح FM-02
خارطة التنفيذ الرسمية، ولا يُعاد بناؤها إلا بتعديل الدستور نفسه.

---

## ملحق — سجل التطوّر
- **v1:** خارطة مراحل تقنية. · **v2:** مراحل مكتملة (شرائح رأسية) بالحقول العشرة.
- **v3:** مجالات أعمال (المحرّك مجالٌ أخير). · **v4:** المحرّك تحوّل إلى **طبقة تنفيذ** لا مجال؛
  أربعة مجالات مستقلّة فوقها؛ مصفوفة ملكية بلا ازدواج؛ تعريف اكتمال موحّد (13 بنداً)؛ خارطة تبعيات.
  FM-01 دون مساس؛ البوابات الأربع وقواعد الأمان/العكس/الاختبارات وكل القرارات محفوظة.

**تحقّق مستقل (Fable 5) على v4: PASS WITH NOTES** — المعمارية صحيحة (مجالات فوق طبقة؛ المحرّك
طبقةٌ لا مجالاً؛ لا ازدواج ملكية؛ FM-01 لم يُمَسّ). **دُمِجت ست تصويبات في هذه النسخة:**
(A) «مستقلّة 100%» ← «مستقلّةٌ على مستوى الكود، مقترنةٌ فقط بعقد الحدث المجمَّد»؛ وفصلُ الالتقاط
إلى تعريف(طبقة)/إطلاق(مجال)/تخزين(طبقة). · (B) الطبقة نواةٌ مشتركة تحمل قواعد دستورية لا سباكة. ·
(C) قواعد التخصيص دستورية ومحرّكها طبقة؛ الغداء يملك الالتقاط المُطلِق فقط. · (D) **إعادة التصنيف
نُقلت إلى الطبقة** كعمليةٍ محكومة (الاستثناء الوحيد لقاعدة القراءة-فقط). · (E) أُضيف مسار كتابة
إعادة التصنيف لخارطة التبعيات ونُزع الإطلاق المطلق لـ«بايتية». · (F) الأساس-أ يُثبّت دلالات
الانحرافات الثلاثة؛ الأساس-ب مطابقٌ بايتياً على **مظروف الإنتاج** بانحدارٍ مسمّى.
