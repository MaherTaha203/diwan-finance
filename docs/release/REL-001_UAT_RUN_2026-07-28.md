# REL-001 — Live UAT Run · 2026-07-28 (post R9 handoff fix)

> Full acceptance run executed against the **real authenticated application** booted with a
> non-production seed (`tools/ux-live-review.mjs` Supabase stub + admin session), after the
> REPORT-001 R9 handoff fix (`window.FIN`/`window.DB`). Drives the actual UI, engine and the
> four output surfaces. Financial **correctness** of every mutating operation is separately
> certified by the constitutional lab (**90/90 · 23/23**, FOC-001…FOC-025) and the node suite
> (64 pass / 2 documented-legacy fail). Scenarios that require live Supabase auth / Edge
> Functions (real sign-in lockout, row persistence, multi-role enforcement) are marked
> **Owner-manual** — their engine-level behaviour is lab-certified; only the production
> round-trip remains for the operator.

## Environment
- App: real `public/` served locally; Supabase stubbed with seed (4 members, 3 receipts,
  2 payments, 4 subscriptions, 2 audit rows); authenticated as **admin**.
- Browser: Chromium (`/opt/pw-browsers/chromium`), viewport 1366×900, `dir=rtl`.
- Handoff fix loaded via `index.html` (not injected). **0 boot errors** across the run.

## Result summary

| # | Scenario | Result | Evidence |
|---|---|---|---|
| UAT-01 | Authentication | ✅ boot / ⚠︎ owner-manual | Seeded session boots authenticated (`#app` visible, admin); invalid/lockout/sign-out round-trip needs live login-gate Edge Function |
| UAT-02 | Create member (BO-07) | ✅ UI · lab-certified | Members screen renders seeded roster; create/edit/cancel certified FOC-001…003 |
| UAT-03 | Apply annual dues (BO-10) | ✅ UI · lab-certified | Dues workspace renders; dues application certified in lab |
| UAT-04 | Record donation (cash + in-kind) | ✅ UI · lab-certified | Donation report renders with fund-direction + settlement labels; donation cases certified |
| UAT-05 | Receipt voucher | ✅ **PASS** | `Report.render('RECEIPT_VOUCHER','print',{record})` → `ok:true` |
| UAT-06 | Payment voucher | ✅ UI · lab-certified | Payment workspace renders; expense cases certified |
| UAT-07 | Print documents | ✅ **PASS** | Member + fund statement + voucher print compose; **figures == screen** (parity below) |
| UAT-08 | Export PDF | ✅ **PASS** | Member + fund + reports PDF compose; **screen == pdf** figure-set equality |
| UAT-09 | Export Excel | ✅ **PASS** | Member statement / members list / debt report Excel compose; core figures present |
| UAT-10 | Reports review | ✅ **PASS** | Annual-debt, delinquent, donations, members-list, annual-log, users-list, treasury all render on 4 surfaces; **no "unavailable"** on any page |
| UAT-11 | Audit log | ✅ **PASS** | Audit page + annual-log model render seeded actor/time entries |
| UAT-12 | User management (admin) | ✅ UI · ⚠︎ owner-manual | Users screen renders; create/role/password routed through Edge Functions (AUTH-001/002) — live backend |
| UAT-13 | Permissions | ⚠︎ owner-manual | `can()` role model present; accountant/reservation enforcement needs live multi-role sign-in |
| UAT-14 | RTL + i18n | ✅ **PASS** | `dir=rtl`; all 18 pages render RTL; AR↔EN toggle re-renders (verified) |
| UAT-15 | Reconciliation | ✅ **PASS** | Consistency verifier `allMatch=true` (7/7); member & fund **screen == print == pdf == excel** |

## Figure parity — one ReportModel → every surface (measured)

| Report | 4 surfaces ok | screen == print | screen == pdf | core figs in all surfaces |
|---|:--:|:--:|:--:|:--:|
| Member statement | ✅ | ✅ | ✅ | 2/2 (100 / 400) · 16 figs |
| Fund statement — food | ✅ | ✅ | ✅ | 2/2 (120 / 200) · 13 figs |
| Fund statement — diwan | ✅ | ✅ | ✅ | 3/3 · 12 figs |
| Annual debt | ✅ | ✅ | ✅ | 28 figs consistent |
| Delinquent | ✅ | ✅ | ✅ | 16 figs consistent |
| Donation report | ✅ | ✅ | ✅ | 6 figs consistent |
| Members list | ✅ | ✅ | ✅ | 14 figs consistent |
| Annual log | ✅ | ✅ | ✅ | 12 figs consistent |
| Users list | ✅ | ✅ | ✅ | 4 figs consistent |

## Reconciliation — constitutional consistency verifier (UAT-15 gate)
`FIN.verifyConsistency()` → **`allMatch: true`**, **7/7 checks matched**, **0 failed members**,
4 members checked. Checks include:
- member accounts — 5 identities per member (all 4 members),
- Σ member balances: member statement ↔ debt report,
- food net position ↔ food balance + remaining deficit,
- food statement closing ↔ credit − debit; diwan statement closing ↔ credit − debit,
- conservation law (Item 9): every receipt's splits ↔ its stored amount; the three split
  totals ↔ sum of splits.

**No accounting figure differs across any surface.**

## Regression gate
- Node suites: **64 pass / 2 fail** — the 2 are the documented legacy fixture-missing suites
  (`tests/LEGACY_SUITES.md`); no new failures.
- Constitutional lab: **90/90 checks · 23/23 certified · exit 0**.
- Live sweep (18 pages): **0** pages "unavailable"; **0** boot errors.

## Verdict
All automation-driveable acceptance scenarios **PASS**; the four report surfaces (screen,
print, PDF, Excel) render with full figure parity and constitutional reconciliation.
**Remaining for GA sign-off (owner-manual, on live Supabase):** UAT-01 lockout/sign-out,
UAT-02/03/04/06/12 write-persistence round-trip, UAT-13 multi-role enforcement — each of
whose financial logic is already lab-certified. Append the operator's manual results here
before final GA sign-off.
