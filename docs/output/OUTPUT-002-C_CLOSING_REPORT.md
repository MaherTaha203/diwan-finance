# OUTPUT-002-C — تقرير الإغلاق (Closing Report)

**النطاق:** طبقة الإخراج وتجربة المستخدم فقط — **لا تغيير في أي منطق مالي أو في FIN/DB/المحاسبة**.
**الفرع:** `claude/output-002c` · **الطلب المرجعي (PR):** #250
**التاريخ:** 2026-07-28

هذا التقرير يثبت النقاط الأربع المطلوبة قبل إغلاق OUTPUT-002-C، ويُفصح صراحةً عن أي بقايا Legacy مع سبب بقائها وخطة إزالتها.

---

## القرارات المعتمدة (كما طلب المالك)

1. زر واحد فقط **«الإخراج ▼»** في جميع الصفحات، قائمته: 🖨 طباعة · 📄 PDF · 📊 Excel · 🔗 نسخ الرابط · 📤 مشاركة · ⚙ إعدادات الإخراج — ويُمنع أي زر طباعة/PDF/Excel منفصل خارجه.
2. بناء **Organization / Output Profile** كاملًا الآن (بيانات المؤسسة تُترك افتراضية/فارغة ليملأها المالك لاحقًا).
3. **حذف CSV** بالكامل.
4. **رمز QR** يمثّل Deep Link داخليًا للتقرير، ويعتمد نظام الدخول والصلاحيات الحالي؛ لا روابط عامة/رموز وصول (تبقى ضمن OUTPUT-003). QR و«نسخ الرابط» و«المشاركة» جميعها تستخدم **نفس الرابط** (`ReportDeepLink.current()`).

---

## البند 1 — جميع صفحات النظام تستخدم زر «الإخراج ▼» نفسه ✅

مكوّن واحد مشترك (`output-menu.js` → `OutputMenu.build`) يعيد استخدام قشرة القائمة المنسدلة العامة (`.export-dropdown` + `togglePageExport` + الإغلاق بالنقر خارجها)، ويُستهلك من:

- **شريط محرّك التقارير** (الكشوف): `Report.outputButtons` يبني نفس القائمة (C7a).
- **رؤوس صفحات القوائم/التقارير** الـ 11 (إيصالات/مصاريف الغداء والديوان، التبرعات، الأعضاء، المديونية السنوية، المتأخرون، الاشتراكات السنوية، المستخدمون، سجل العمليات) (C7b).

**دليل قابل للتحقّق (index.html):**

| المؤشر | القيمة |
|---|---|
| قوائم «الإخراج ▼» (`export-dropdown out-menu`) | **11** |
| بقايا «تصدير ▼» | **0** |
| أزرار طباعة منفصلة في رؤوس الصفحات (`prtX`) | **0** (أُزيلت 5: `prtDonStmt`, `prtMembersList`, `prtAnnualDebt`, `prtDelinquent`, `prtAnnual`) |

- عناصر القائمة متطابقة في كل مكان؛ «إعدادات الإخراج» **للمشرف فقط** (يُخفى لغير المشرف عبر `body.is-admin` + بوابة عند النقر).
- قائمة لوحة المعلومات `#dash-new-menu` هي مكوّن **إنشاء سريع** (سند/عضو جديد) وليست زر إخراج — تُركت كما هي عن قصد.
- تم التحقّق في Chromium: القائمة الحيّة تحوي العناصر الستة؛ «نسخ الرابط» يضع الرابط العميق في الحافظة؛ «إعدادات الإخراج» يفتح لوحة الملف؛ لا أخطاء إقلاع.

---

## البند 2 — جميع التقارير تستخدم نفس ReportModel ✅

- ملف التفعيل `report-activation.js` (R8-a) يُفعّل **جميع أعلام المحرّك (15 علمًا) ON عند التحميل** (`root[flag] = true`)، فيصبح المحرّك الموحّد هو المسار **الحيّ الافتراضي** لكل عائلة تقارير: `Report.render(model, target)` فوق **نموذج واحد** لكل تقرير (`ReportModels.*`).
- الكشوف تمرّ عبر شريط المحرّك مباشرة؛ صفحات القوائم تمرّ عبر cutovers (`ReportCutoverLists/Debt/Donation` + `__voucherListDeliver`) التي تستدعي نفس `Report.render`.
- الأعلام قابلة للإرجاع لحظيًا (kill-switch) دون إعادة نشر — مسار الطوارئ فقط.

---

## البند 3 — تكافؤ البيانات في المخارج (Screen / Print / PDF / Excel) ✅

بوّابة الفحص الحيّة (Chromium) تقارن **مجموعة الأرقام** المستخرجة من كل سطح لكل تقرير:

| التقرير | Screen==Print | Screen==PDF | أرقام جوهرية في كل الأسطح |
|---|:--:|:--:|:--:|
| MEMBER_STATEMENT | ✅ | ✅ | 2/2 |
| FUND (غداء) | ✅ | ✅ | 2/2 |
| FUND (ديوان) | ✅ | ✅ | 3/3 |
| ANNUAL_DEBT | ✅ | ✅ | — |
| DELINQUENT | ✅ | ✅ | — |
| DONATION_REPORT | ✅ | ✅ | — |
| MEMBERS_LIST | ✅ | ✅ | — |
| ANNUAL_LOG | ✅ | ✅ | — |
| USERS_LIST | ✅ | ✅ | — |
| TREASURY_POSITION | ✅ | ✅ | 3/3 |

- **`unavailable = 0` · `bootErrors = 0` · السند (voucher) = OK**.
- رمز QR جزء من «القرطاسية» (chrome) لا من البيانات؛ لذا يُستبعَد رابطه من فحص تطابق الأرقام (الشاشة بلا QR، والطباعة/PDF بها) — تكافؤ **البيانات** قائم.
- اختبارات Node: **66 ناجحة / 2 فاشلة** (الفاشلتان قديمتان وموثّقتان: `business-operations-slice1`, `constitutional-explicit-q5` — غير متعلقتين بطبقة الإخراج).

---

## البند 4 — مسارات Legacy في منظومة الإخراج

**المسار الحيّ للإخراج خالٍ من Legacy:** كل إخراج يبدأه المستخدم (عبر «الإخراج ▼») يمرّ حصريًا عبر المحرّك بينما الأعلام ON. **CSV أُزيل بالكامل** (زر + مُصدّرات + هدف المحرّك + المسارات القديمة + الكود غير المستخدم) بلا مراجع مكسورة.

**تبقّى — يجب الإفصاح عنه صراحةً:**

1. **فروع Legacy احتياطية خامدة داخل `exportPagePDF` / `exportPageExcel`** (بُناة جداول قديمة للتبرعات/الأعضاء… إلخ).
   - **الحالة:** **غير قابلة للوصول** أثناء تشغيل الأعلام (فروع المحرّك في الأعلى تُرجِع قبل بلوغها).
   - **سبب البقاء:** مُحتفَظ بها **بقرار تصميم R8-a** كـ kill-switch للإرجاع الطارئ لسطح واحد دون إعادة نشر (سياسة «يُزال بعد سماح/تحقّق وموافقة المالك»، تمامًا كما أزال R8-b بُناة الكشوف/القوائم وCSV).
   - **الخطة:** إزالتها في تنظيف مخصّص على غرار R8-b بعد فترة استقرار وموافقة المالك على إسقاط مسار الإرجاع.

2. **تصدير JSON لكشف العضو** (`exportMemberStmt('json')`).
   - **الحالة:** ليس ضمن قائمة «الإخراج ▼»، ولا يُستدعى من أي زر في الواجهة (كود خامد فعليًا)، وخارج بوّابة التكافؤ.
   - **سبب البقاء:** خارج نطاق OUTPUT-002-C (المالك حدّد حذف CSV فقط).
   - **الخطة:** حذفه أو توجيهه عبر المحرّك في دفعة لاحقة عند الطلب.

> لا توجد مسارات Legacy **حيّة** في منظومة الإخراج؛ البند المتبقّي الوحيد ضمن المسار المرئي للمستخدم كان CSV — وقد حُذف.

---

## ملخّص التغييرات

| الملف | التغيير |
|---|---|
| `report-deeplink.js` (جديد) | مصدر الرابط العميق الداخلي الوحيد + توجيه + العودة بعد الدخول |
| `report-share.js` (جديد) | نسخ الرابط + المشاركة + فتح الإعدادات (تفويض على مستوى المستند) |
| `output-profile.js` (جديد) | مخزن Organization/Output Profile + واجهة قراءة موحّدة |
| `output-settings.js` (جديد) | لوحة «إعدادات الإخراج» (للمشرف) |
| `output-menu.js` (جديد) | مكوّن «الإخراج ▼» المشترك |
| `report-engine.js` | حذف CSV؛ `outputButtons` يبني القائمة المنسدلة |
| `report-layout.js` | قراءة الهوية من الملف؛ صفّ التوقيع/الختم/QR (طباعة/PDF) |
| `report-cutover*.js` | حذف فروع CSV |
| `reports.js` | حالة فلترة المديونية/المتأخرين في الرابط + مُلحقات القراءة |
| `app.js` | حذف `exportCSV` + فرع CSV؛ `pageOutput(type,target)` |
| `auth.js` | العودة بعد الدخول (`resume`)؛ `body.is-admin`؛ تنظيف مُحدِّدات CSV |
| `i18n.js` | مفاتيح `output_dd/copy_link/share/output_settings`؛ حذف CSV |
| `index.html` | توحيد 11 قائمة إخراج؛ حذف 5 أزرار طباعة منفصلة؛ تسجيل السكربتات |
| `tests/*` | اختبارات deeplink + output-profile الجديدة؛ تحديث اختبارات CSV/المحرّك |

**العمولات (commits):** أسماء ملفات مقروءة (12) · روابط عميقة + العودة (13) · نسخ/مشاركة (14/15) · حالة الفلترة في الروابط (16) · حذف CSV (C4) · الملف والقراءة الموحّدة (C5) · لوحة الإعدادات (C6) · توحيد الزر (C7a/C7b) · QR/التوقيع/الهوية (C8).

---

## ما يتبقّى على صاحب النظام

القِيَم الافتراضية جاهزة والبنية كاملة. لإظهار الهوية الرسمية على المستندات، افتح **«الإخراج ▼» → إعدادات الإخراج** (كمشرف) واملأ: الشعار، الختم، صورة التوقيع، العنوان/الهاتف، واسم/صفة الموقّع، وفعّل ما يلزم (الشعار/التوقيع/الختم/QR). تُحفظ محليًا على الجهاز.
