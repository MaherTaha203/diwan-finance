# DOMAIN CLOSURE REPORT — DCR-01
# المجال 1 — إدارة الديوان (Diwan Management)

> **الحالة:** مُغلَق رسمياً باعتماد المالك (2026-07-13). **دُمج ضمن PR #37 → `main`.**
> **شروط إعادة الفتح (حصراً):** إصلاح خلل · تعديل دستوري على FM-01 · تعديل رسمي على FM-02 · طلب أعمال جديد من المالك. ولا يُفتح لأي سبب آخر.

| # | البند | الإجابة |
|---|---|---|
| 1 | **ما الذي أُنجز** | فصلُ إيصال الديوان إلى حدثين أوّليين مستقلّين: **FE-004 إيراد تشغيلي** (مقابل خدمة) و**FE-005 تبرع نقدي** (هبة)، كلاهما يزيد خزينة الديوان. التشغيلي **مستبعَد** من السجل النقدي وصفحة التبرعات؛ التبرع **مُدرَج**. غطّى: الإدخال (منتقي النوع) · كشف الصندوق (وسمُ النوع) · سند القبض (صف «نوع الحدث») · تصدير CSV (عمود «نوع الحدث»). |
| 2 | **ما الذي لم يتغيّر** | الإيصالات التاريخية للديوان (لا إعادة تصنيف) · محرّكا `fin.js`/`fin2.js` سلوكاً وبصمةً على السطور القديمة · المجالات الأخرى (الغداء/التبرعات/الحسابات) · قاعدة الإنتاج (لا سند مُمَسّ) · النوع التشغيلي إضافةٌ للمستقبل لا تغييرٌ رجعي. |
| 3 | **عدد الملفات المعدَّلة** | **6** — `index.html` · `forms.js` · `crud.js` · `fin.js` · `print.js` · `app.js`. (إجمالي +17/−2 سطراً، إضافيٌّ بالكامل.) |
| 4 | **عدد الملفات الجديدة** | **0** (في شيفرة الإنتاج). اختبار المجال `d1-diwan.cjs` أداةُ تحقّقٍ خارج المستودع. |
| 5 | **هل تغيّر أي رقم مالي؟** | **لا.** خط الأساس مطابقٌ بايتياً: الديوان 3,481 · الغداء 1,694.04 · العجز −8,139 · السجل النقدي 7 = 4,700 · العيني 4. |
| 6 | **هل تغيّر أي Migration؟** | **لا.** لا هجرة، لا مساس بأي صفٍّ في قاعدة الإنتاج. |
| 7 | **هل تغيّر أي نموذج بيانات؟** | **لا.** لا تغيير في الجداول/الأعمدة، ولم يُمَسّ `model2.js` في تنفيذ المجال. *(بندَا الكتالوج FE-004/FE-005 عُرِّفا وجُمِّدا سابقاً ضمن تعديل FA-01 الدستوري — بأثرٍ رقميٍّ صفر — لا ضمن هذا المجال.)* |
| 8 | **نتائج الاختبارات** | اختبار المجال 1 (يقود النموذج الحقيقي): **10/10 PASS، 0 أخطاء JS**. حزمة القبول الكاملة: **48/48 PASS، 0 أخطاء صفحة** — بلا انحدار. |
| 9 | **نتائج مراجعة Fable 5** | **PASS — لا معيق للإغلاق.** تأكّد غياب المخاطر الثلاث: (أ) لا سطح يعامل الإيصالات الجديدة كـ`donation_cash` قديم · (ب) تسريب التشغيلي للسجل مستحيلٌ بنيوياً (السجل يُحسب بخاصية `register` لا بالاسم) · (ج) مطابقة البايت للسطور القديمة قائمة. النسخة الاحتياطية تصدّر `movement_type` بلا فقد. الملاحظة الوحيدة (عمود التصدير) **عولِجت** قبل الإغلاق. |
| 10 | **ديون تقنية ناتجة عن هذا المجال؟** | **لا — لا دَينَ جديداً أنتجه المجال 1.** توجد ملاحظةٌ استشرافية واحدة **سابقةٌ للمجال** (لا ناتجةٌ عنه): |

**الملاحظة الاستشرافية (ليست دَيناً من المجال 1):**
- **الوصف:** `reclassifyVoucher` (`crud.js`) يقيّد الوجهة للنوع القديم `donation_cash` فقط، ولا يقيّد `diwan_cash_donation`/`diwan_operational_income` بخزينة الديوان.
- **سبب التأجيل:** خاملٌ اليوم تماماً — لا واجهة إعادة تصنيف مبنيّة، ولا مسار حيّ يبلغه. تحصينه الآن تحسينٌ استباقيٌّ يخالف مبدأ الاستقرار.
- **مجال المعالجة:** **المجال 4 — إدارة السندات وإعادة التصنيف** (حيث تُبنى تلك الواجهة).

---
**الخلاصة:** المجال 1 جعل النظام أوضح (تمييزٌ محاسبيٌّ حقيقيّ بين الدخل والتبرع) دون تغيير أي رقم، ودون ترك عملٍ غير مفهوم. **مُغلَق.**
