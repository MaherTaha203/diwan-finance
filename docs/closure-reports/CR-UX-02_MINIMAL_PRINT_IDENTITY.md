# Closure Report — CR‑UX‑02 · هوية الطباعة البسيطة الموحّدة (Identity v4)
## EB‑07 · UX‑02 — Minimal unified print/export identity

- **النوع:** UX Improvement (Production Evolution). **بصريّ/مخرجاتٌ فقط.**
- **الفرع / PR:** `claude/new-session-51j8hh` · **PR #42** (فوق `main` بعد دمج PR #38 + حزمة العلامة + وحدة الحجوزات).
- **الكوميتان:** `6622ce0` (الطباعة/التصدير) · `ac39ae9` (مطابقة الشاشة).
- **التاريخ:** 2026‑07‑14 · **الحالة:** بانتظار اعتماد المالك النهائي.

---

### 1) ما تغيّر (السطوح)
| المجموعة | التغيير |
|---|---|
| **الطباعة (print.js)** | `PRINT_TOKENS` → Identity v4؛ `reportHeader` جديد (ترويسة بيضاء + شعار داكن بإطار فيروزيّ + خطّ فاصل + عنوان متوسّط بخطٍّ فيروزيّ)؛ جداول خفيفة + عمود رصيدٍ فيروزيّ |
| **التواقيع** | مساعد مشترك `reportDfoot` ⇒ «توقيع الديوان» فقط في كل السطوح (بدل توقيعين) |
| **السندات** | المبلغ بالكلمات إنجليزيّ (`amountToWordsEn`) |
| **كشف العضو (print + screen)** | الرصيد النهائيّ صفٌّ فيروزيٌّ (`tr.final`/`as-ffinal`) يُطابق عمود الرصيد الجاري؛ اسم/رقم العضو مرّة واحدة (أُزيل تكرار `msmeta`) |
| **التقارير (reports.js)** | `prtAnnualDebt`/`prtDelinquent`/`prtDonStmt` + تقرير المطابقة ⇒ `reportDfoot` |
| **التصدير** | `exportMemberStmt` ⇒ `reportDfoot`؛ HTML/PDF يرث Identity v4؛ Excel: تعبئة الرؤوس فيروزيّة (`styleDiwanSheet` NAVY→`0E7C66`) |

### 2) قرارات المالك — كلها مُطبَّقة
✓ الفيروزيّ يحلّ محلّ كل ذهبيّ/أزرق/كُحليّ · ✓ «توقيع الديوان» فقط · ✓ مبلغ السند إنجليزيّ · ✓ اسم/رقم مرّة واحدة · ✓ الرصيد النهائيّ أسفل عمود الرصيد الجاري · ✓ بقاء QR + «أصل» + A4 + فواصل الطباعة.

### 3) إثبات عدم تغيّر الأرقام (byte-parity)
المطابقة البايتية **PASS** — لا رقمَ تغيّر: غذاء **1694.04** · ديوان **3481** · عجز **−8139** · صافي **−6444.96** (FIN‑delegate == FIN2). التغيير في طبقة العرض فقط؛ لا مساس بـ`FIN`/`FIN2`/`FinContract`/`MODEL2` ولا بأي حسابٍ في `reports.js`.

### 4) نتائج الاختبارات
| الاختبار | النتيجة |
|---|---|
| `node --check` (print/reports/app) | نظيف |
| قبول E2E (tests/e2e-acceptance.cjs) | **48/48 · pageerrors=0** |
| المطابقة البايتية (fb1) | **PASS · pageerrors=0** |
| إعادة الإرساء فوق main الجديد | نظيفة (بلا تعارض)، وأُعيد التحقّق بعدها |

### 5) اللقطات البصرية
كشف العضو (شاشة + طباعة)، سند القبض، سجل السندات، تقرير المديونية — هويةٌ واحدة تُطابق المحاكاة المعتمدة من المالك (الفيروزيّ، الترويسة البيضاء، الرصيد النهائيّ يُطابق العمود، توقيع الديوان المفرد، المبلغ الإنجليزيّ).

### 6) مراجعة Fable 5 (REVIEW MODE)
> _(بانتظار الحكم — سؤال: هل يوجد Bug/Blocker يمنع الإغلاق؟)_

### 7) تحسينات مستقبلية (مسجَّلة — لا تُنفَّذ)
- `amountToWordsAr` صار غير مستعمَل بعد تحويل السندات إلى الإنجليزيّ (كودٌ ميّتٌ غير ضارّ).
- توحيد قوائم القبض/الصرف نفسها بصرياً (خارج نطاق EB‑07 — تبقى جداول).

### 8) القيود المعروفة
- الطباعة تعتمد خطوط Google Fonts عبر الشبكة (سلوكٌ قائمٌ لا يُغيّره هذا العمل).

### DoD
تتبنّى كل المخرجات (شاشة/طباعة/تصدير) Identity v4 مع بقاء كل رقمٍ وعنصرٍ رسميٍّ مطابقاً · قبول 48/48 · لقطاتٌ تؤكّد الهوية الواحدة · Fable = PASS ⇒ ثم **اعتماد المالك النهائي والدمج**.
