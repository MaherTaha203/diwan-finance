# Transient machine output regenerated by `node lab/run.cjs`.
# The human evidence summary (results/REPORT.md) and the screenshot archive
# (screenshots/) ARE committed as the permanent evidence baseline.
results/*.json
